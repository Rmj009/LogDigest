from setuptools import setup
from .lib import *

setup(
    name='my_package',
    version='1.0',
    # packages=find_packages(),
    install_requires=[
        # List your dependencies here
    ],
)
